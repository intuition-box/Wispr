// Blueprint output:
//   - Visual gallery (component cards + trust scores)
//   - System Prompt (editable, copy)
//   - MCP Config (generates claude_desktop_config.json)
//   - Packages (install command)
//   - LLMs (recommended models)
//   - One-shot install prompt
//   - Publish on-chain CTA

export default function BlueprintPage() {
  return <main>Blueprint — TODO</main>;
}
